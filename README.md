# boring.ghostty

Ghostty terminal emulator configuration.

## Installation

The setup is intended for macOS and uses Homebrew:

```sh
git clone https://github.com/EskelinenAntti/ghostty-configuration.git ~/.config/ghostty
cd ~/.config/ghostty
brew bundle
```

Install Ghostty with `brew bundle`.
